import { defineConfig } from 'vite';

// https://vitejs.dev/config
// For production builds, the Next.js 'out' directory will be served
export default defineConfig({
    // Renderer process uses the built Next.js output
});

